const assign = require('chai').assign;

const Solver = require('./solver');
const Grid = require('../grid/grid');
const Cell = require('../cell/cell');

describe('Class: Solver', () => {
    describe('Methods', () => {
        describe('setGridValues', () => {
            it('Should call the assignValue method of its grid for every values', () => {
                
            });

            it('Should have set all the values on the grid', () => {
                
            });
        });

        describe('search', () => {
            
        });
    });
});